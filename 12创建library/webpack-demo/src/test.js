// // require('lodash') // 会默认的去node_modules里面查找lodash，不过必须要有
// const webpackNumbers = require('../dist/webpack-numbers')
import * as webpackNumbers from '../dist/webpack-numbers'

console.log('webpackNumbers:', webpackNumbers)

// console.log('webpackNumbers.numToWord:', webpackNumbers.numToWord(2))
